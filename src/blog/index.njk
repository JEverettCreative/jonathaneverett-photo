---
layout: layouts/base.njk
title: Blog
metaTitle: "Blog — Photography Tips, Behind the Scenes, and More"
metaDescription: "Thoughts on photography, behind-the-scenes from shoots, competition tips, and stories from the field. By Jonathan Everett, Nashville photographer."
---

<section class="page-hero">
  <div class="container">
    <h1 class="fade-in">Blog</h1>
    <p class="tagline fade-in">Thoughts, behind-the-scenes, and stories from the field.</p>
  </div>
</section>

<section class="section">
  <div style="max-width: 700px; margin: 0 auto; padding: 0 var(--space-md);">
    <div class="post-list">
      {% for post in collections.posts %}
      <article class="post-item fade-in" style="margin-bottom: var(--space-lg); padding-bottom: var(--space-lg); border-bottom: 1px solid rgba(255,255,255,0.08);">
        <p class="post-date" style="font-size: 0.85rem; color: var(--accent); letter-spacing: 0.05em; margin-bottom: var(--space-xs);">{{ post.date | date("%B %d, %Y") }}</p>
        <h3 style="margin-bottom: var(--space-xs);"><a href="{{ post.url }}" style="color: var(--text); text-decoration: none;">{{ post.data.title }}</a></h3>
        <p class="post-excerpt" style="line-height: 1.75; color: var(--text-muted);">{{ post.data.excerpt }}</p>
        {% if post.data.tags %}
        <div class="post-tags" style="margin-top: var(--space-sm);">
          {% for tag in post.data.tags %}
          <span class="post-tag">{{ tag }}</span>
          {% endfor %}
        </div>
        {% endif %}
      </article>

      {% if loop.index == 1 %}
      <div class="fade-in" style="margin: var(--space-lg) 0; text-align: center;">
        <img src="/images/physique-color/arnold_04277-800w.webp" alt="Physique photography" style="max-width: 100%; max-height: 600px; object-fit: cover; border-radius: 4px;">
      </div>
      {% endif %}

      {% if loop.index == 3 %}
      <div class="fade-in" style="margin: var(--space-lg) 0; text-align: center;">
        <img src="/images/physique-color/dsc04836-800w.webp" alt="Physique photography" style="max-width: 100%; max-height: 600px; object-fit: cover; border-radius: 4px;">
      </div>
      {% endif %}

      {% else %}
      <div class="text-center fade-in" style="padding: var(--space-xl) 0;">
        <p style="color: var(--text-muted); font-family: var(--serif); font-style: italic; font-size: 1.1rem;">New posts coming soon. Stay tuned.</p>
      </div>

      <div class="fade-in" style="margin: var(--space-lg) 0; text-align: center;">
        <img src="/images/physique-color/arnold_04277-800w.webp" alt="Physique photography by Jonathan Everett" style="max-width: 100%; max-height: 600px; object-fit: cover; border-radius: 4px;">
      </div>

      <div class="fade-in" style="margin: var(--space-lg) 0; text-align: center;">
        <img src="/images/physique-color/dsc04836-800w.webp" alt="Physique photography" style="max-width: 100%; max-height: 600px; object-fit: cover; border-radius: 4px;">
      </div>

      {% endfor %}
    </div>
  </div>
</section>
